services:
  playwright:
    image: kyokley/playwright
    hostname: playwright
    environment:
      - DJANGO_SETTINGS_MODULE=mysite.docker_settings
    build:
      context: .
      target: playwright
    depends_on:
      - mediaviewer
    volumes:
      - .:/code
    networks:
      - mediaviewer-backend
